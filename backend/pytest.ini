[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts = -q --disable-warnings --ds=config.settings.test
filterwarnings =
    ignore::DeprecationWarning
